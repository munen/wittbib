collection.each do |book|


  json << [book.authors,
           book.title,
           book.isbn,
           link_to("Ändern", edit_book_path(book), :class => "btn small"),
           link_to("Löschen", book_path(book), :class => "btn small", :method => :delete, :confirm => "Sind Sie sicher?"),]
end
